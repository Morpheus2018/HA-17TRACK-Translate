type: markdown
content: |-
  ## <center> Zugestellt
  ***

  {% if states('sensor.seventeentrack_packages_in_transit') == '0' %} Keine Pakete in Zustellung.
  {% elif states.sensor.deepl_translation_in_transit is none %} Keine Übersetzung.
  {% else %}{% for package in states.sensor.seventeentrack_packages_in_transit.attributes.packages %}
  **{{ package.friendly_name }} ({{ package.tracking_number }}):**
  {{ state_attr('sensor.deepl_translation_in_transit' ~ loop.index0, 'translations')[0]['text'] }} {{ package.location }} am {{ as_timestamp(package.timestamp) | timestamp_custom('%d.%m.%Y %H:%M') }} Uhr.
  ***
  {% endfor %}
  {% endif %}
